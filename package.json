{
  "name": "spoilguard",
  "version": "0.1.0",
  "description": "",
  "private": true,
  "type": "module",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build": "esbuild src/content.js src/background.js src/options.js src/popup.js --bundle --outdir=dist && cp src/veil.css dist/veil.css && cp src/options.html dist/options.html && cp src/popup.html dist/popup.html",
    "watch": "esbuild src/content.js --bundle --outfile=dist/content.js --watch",
    "test": "vitest run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.28.1",
    "jsdom": "^29.1.1",
    "vitest": "^4.1.9"
  }
}
